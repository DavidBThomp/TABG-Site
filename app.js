// Get all the cells on the board
const cells = document.querySelectorAll('.cell');

// Set up an array to hold the numbers for the game
const numbers = [];

// Generate a new game
function newGame()
